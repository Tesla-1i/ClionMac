#include <iostream>
#include <string>
//#include <altivec.h>
#include <vector>
//class string;

class vector;

int main() {
    //std::cout << "Hello, World!" << std::endl;
    //return 0;

    struct Pair{
        std::string name;// using string in head file!
        double val;
    };

    vector<Pair>pairs;
    double& value(const std::string& s)
    {
        for (int i = 0; i < pairs; ++i) {

        }
    }

}